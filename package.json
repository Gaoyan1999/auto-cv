{
  "name": "@auto-cv/root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter @auto-cv/web dev",
    "build": "pnpm --filter @auto-cv/web build",
    "format": "pnpm --filter @auto-cv/web format",
    "lint": "pnpm --filter @auto-cv/web lint",
    "preview": "pnpm --filter @auto-cv/web preview"
  },
  "devDependencies": {}
}
